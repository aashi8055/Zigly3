<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY.

  Two jobs:

  1. Trust the corporate proxy CA. This machine sits behind Netskope TLS
     inspection, which re-signs every certificate. Windows trusts that CA; the
     Android emulator's trust store does not, so the WebView refused zigly.com
     with ERR_CERT_AUTHORITY_INVALID. We add that one CA *alongside* the system
     anchors — certificate validation stays fully enabled.

  2. Let Metro through. The base config forbids cleartext, which also blocked
     the dev server (plain http on 10.0.2.2:8081) and left the app unable to
     load its JS bundle. The exemption below is scoped to the loopback hosts
     the packager uses, and nothing else.

  We deliberately do NOT override onReceivedSslError, and do NOT trust
  arbitrary user-installed CAs.

  This file is in src/debug, so release builds never see it: they use the
  platform default of system CAs only, with cleartext denied everywhere.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="@raw/corporate_proxy_ca" />
        </trust-anchors>
    </base-config>

    <!-- Metro dev server only. 10.0.2.2 is the emulator's alias for the host. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
